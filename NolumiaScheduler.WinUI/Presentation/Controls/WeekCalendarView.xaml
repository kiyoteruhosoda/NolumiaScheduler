<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="NolumiaScheduler.WinUI.Presentation.Controls.WeekCalendarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="Root">

    <Grid x:Name="RootGrid" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="56"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Header row: day labels -->
        <Grid x:Name="WeekHeaderGrid" Grid.Row="0" Grid.Column="1" ColumnSpacing="1" Padding="0,2,0,6"/>

        <!-- All-day lane -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="終日" FontSize="10"
                   VerticalAlignment="Center" Margin="2,0"/>
        <Grid x:Name="WeekAllDayGrid" Grid.Row="1" Grid.Column="1"
              MinHeight="28" ColumnSpacing="1" Background="#1AF5F5F5"/>

        <!-- Scrollable body -->
        <ScrollViewer x:Name="WeekScroll" Grid.Row="2" Grid.ColumnSpan="2"
                      VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      ViewChanged="OnWeekScrollViewChanged">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="56"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Time labels column -->
                <ItemsControl x:Name="TimeSlotList" Grid.Column="0"/>

                <!-- Day body columns -->
                <Grid x:Name="WeekBodyGrid" Grid.Column="1" ColumnSpacing="1"
                      SizeChanged="OnWeekBodyGridSizeChanged"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
